import React from 'react';
import ReactDOM from 'react-dom';
import Layout from './Resume-Components/Layout'

const resumeDiv = document.getElementById('resume-container');

ReactDOM.render( <Layout />, resumeDiv);